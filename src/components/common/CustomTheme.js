import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main:'#1ca9d0'
        },
        secondary: {
            main:'#FE6B8B',
        },
        inherit: {
            main:'#fffbfb',
        }
    },
});

export default theme;